general:
  branches:
    ignore:
      - gh-pages
database:
  override:
    - echo "Skipping DB section."
machine:
  timezone: Asia/Tokyo
  node:
    version: 8.5.0
test:
  override:
    - npm test:
        parallel: true
    - 'aws s3 cp $CIRCLE_ARTIFACTS/ s3://$S3_BUCKET_URL/ss/$DESTINATION_FOLDER --recursive --acl public-read':
        parallel: true
